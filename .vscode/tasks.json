{
  "tasks": [
    {
      "label": "install nnstreamer",
      "command": "/usr/bin/ninja",
      "args": [
        "install",
        "-C",
        "${workspaceFolder}/build"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      }
    }
  ],
  "version": "2.0.0"
}
